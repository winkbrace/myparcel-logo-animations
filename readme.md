# MyParcel CSS Logo animations

This is a small hobby project to create animations for the MyParcel logo using only SVG and CSS.

## Run

To run the animations, simply open the `index.html` in your browser.
